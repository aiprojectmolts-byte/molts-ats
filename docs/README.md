# docs/

このフォルダには以下のドキュメントを配置してください。

- `admin-manual.docx` — 管理者マニュアル
- `user-manual.docx` — ユーザーマニュアル
- `ats-pitch.pptx` — クライアント向け機能紹介
- `wireframe-guide.docx` — ワイヤーフレーム手順書

配置後に次のコマンドでコミットします。

```sh
cd ~/Documents/scout/ats
git add docs/
git commit -m "docs: 各種ドキュメントを追加"
```
